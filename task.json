{
  "type": "codex.task.v1",
  "repoPath": ".sandbox",
  "branch": "chore/codex-real",
  "instructions": "You MUST return a unified diff patch in 'suggested_patch_unified' fixing the double semicolon and adding TS types to mul(a:number,b:number). No refactors.",
  "atIds": [
    "AT-CODEX-REAL-001"
  ],
  "diffOrFiles": [
    "diff --git a/calc.ts b/calc.ts",
    "index 43a02b8..9ab29f5 100644",
    "--- a/calc.ts",
    "+++ b/calc.ts",
    "@@ -2 +2,4 @@ export function add(a:number,b:number){",
    "-  return a + b",
    "+  return a + b;;",
    "+}",
    "+export function mul(a,b){ // missing types, style problem",
    "+  return a*b"
  ]
}
